import { Animation } from '@ionic/core';
export declare namespace ChainAnimations {
    function chain(animations: Animation[], stagger?: number, limitStagger?: boolean, limitCount?: number): Animation;
}
