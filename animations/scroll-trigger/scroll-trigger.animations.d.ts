import { Animation } from '@ionic/core';
export declare namespace ScrollTriggerAnimations {
    function oneWayScrollTrigger(trigger: HTMLElement, scroller: HTMLElement, animation: Animation, triggerOffset?: number, markers?: boolean): void;
}
